# Core MCP server
mcp>=1.26.0
requests>=2.31.0
starlette>=0.40.0
uvicorn>=0.40.0
pydantic>=2.0.0

# Zotero integration
pyzotero>=1.5.0

# Search reranking
rank_bm25>=0.2.2

# Embedding models for local semantic reranking
sentence-transformers>=3.0.0
onnxruntime>=1.17.0
optimum[onnxruntime]>=1.17.0
numpy>=1.24.0

# Testing
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Thin-client search stack (no-JVM serving backend)
tantivy>=0.26.0
bmp>=0.2.6
usearch>=2.20.0
zstandard>=0.22.0
orjson>=3.9.0

# Storage/compression evals
brotli>=1.1.0
py3langid>=0.3.0
